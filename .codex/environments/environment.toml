# THIS IS AUTOGENERATED. DO NOT EDIT MANUALLY
version = 1
name = "py-qgate"

[setup]
script = ""

[setup.win32]
script = '''
cd "$CODEX_WORKTREE_PATH"

# Ensure uv is installed
if (-not (Get-Command uv -ErrorAction SilentlyContinue)) {
    powershell -ExecutionPolicy ByPass -c "irm https://astral.sh/uv/install.ps1 | iex"
}

# Ensure uv is in session PATH
$env:Path = "$env:USERPROFILE\.local\bin;$env:Path"

# Sync virtual environment cleanly (fast, no lint noise)
uv sync --all-extras'''

[cleanup]
script = ""

[cleanup.win32]
script = '''
cd "$CODEX_WORKTREE_PATH"
Remove-Item -Recurse -Force -ErrorAction SilentlyContinue .ruff_cache, .pytest_cache'''
