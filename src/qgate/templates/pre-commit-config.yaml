repos:
  - repo: local
    hooks:
      - id: qgate
        name: Quality Gate
        language: system
        entry: uvx --from git+https://github.com/rhythmatician/py-qgate qgate --fix --type-checker dmypy
        types: [python]
        pass_filenames: true
